import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.oguzhoca.germanlearning',
  appName: 'Deutsch Lernen',
  webDir: 'www'
};

export default config;
